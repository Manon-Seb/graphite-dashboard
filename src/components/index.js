

/*
const wrapper = (urlPath, idFunction) => {
  console.log("You're in the wrapper");
  if (idFunction === 1) {
    const request = () => {
      fetch(mainUrl + urlPath)
        .then((response) => response.json())
        .then((data) => console.log(data))

      return request
    }
  }

  if (idFunction === 2) {
    const request = () => {
      fetch(mainUrl + urlPath)
        .catch(err => console.log(err))

      return request
    }
  }
} */
